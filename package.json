{
  "name": "bizual-ai-web",
  "version": "1.0.0",
  "description": "Bizual.ai landing page + HubSpot lead capture",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required — static files served by Express'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "compression": "^1.7.4",
    "express-rate-limit": "^7.4.0",
    "resend": "^4.0.1"
  }
}
